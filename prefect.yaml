# Generic metadata about this project
name: quanterra-pipeline
prefect-version: 3.1.15

# build section allows you to manage and build docker images
build:
- prefect_docker.deployments.steps.build_docker_image:
    id: build_image
    requires: prefect-docker>=0.3.1
    image_name: "europe-west2-docker.pkg.dev/quanterra/quanterra-docker/market-data-ingestion"
    tag: latest
    dockerfile: Dockerfile

# push section allows you to manage if and how this project is uploaded to remote locations
push:
- prefect_docker.deployments.steps.push_docker_image:
    requires: prefect-docker>=0.3.1
    image_name: '{{ build_image.image_name }}'
    tag: '{{ build_image.tag }}'
    # credentials: '{{ prefect.blocks.docker-registry-credentials.gcp-artifact-registry }}'

# pull section allows you to provide instructions for cloning this project in remote locations
pull: null


# the deployments section allows you to provide configuration for deploying flows
deployments:
- name: quanterra-pipeline
  version: null
  tags: []
  description: "Daily market data ingestion pipeline"
  schedule: {cron: "0 5 * * *"}
  flow_name: run_market_data_ingestion
  entrypoint: src.flows.market_data:run_market_data_ingestion
  # parameters: {}
  work_pool:
    name: cloud-pool
    work_queue_name: default
  job_variables:
    image: '{{ build_image.image_name }}:{{ build_image.tag }}'
